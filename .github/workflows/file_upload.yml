name: dosya yükleme

on:
  watch:
    types: [started]
    
jobs:
  build:
    runs-on: ubuntu-latest


    steps:
       - name: Checkout
         uses: actions/checkout@master
           
       - name: Initializing environment
         run: |
            sudo curl https://android.googleapis.com/packages/ota-api/package/10fbe44c6e665ce1bea84f6ae2acc0cc0a403403.zip --output gm8_ota.zip
            sudo curl https://raw.githubusercontent.com/Mikubill/transfer/master/install.sh --output install.sh
            sudo chmod +x install.sh
            sudo ./install.sh
            sudo ./transfer gof gm8_ota.zip
